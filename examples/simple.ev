/* comment */

/* event declaration */
event SampleEvent { pos, time };
event InheritanceEvent extends SampleEvent { angle };
event OtherEvent { foo };


InheritanceEvent constfun(SampleEvent a) :=
{
	pos = a.pos + [(1+1)*2, 3],
	time = 4 * [39] + a.time,
    angle = 4 * ([2,3] + [1,1])
};
